Elassandra has been deployed.

Search API:
  kubectl port-forward svc/{{ include "elassandra.fullname" . }}-search 9200:{{ .Values.searchService.port }}

CQL:
  kubectl port-forward svc/{{ include "elassandra.fullname" . }}-cql 9042:{{ .Values.cqlService.port }}

{{- if .Values.dashboards.enabled }}
OpenSearch Dashboards:
  kubectl port-forward svc/{{ include "elassandra.fullname" . }}-dashboards 5601:{{ .Values.dashboards.service.port }}
{{- end }}

Minikube preset example:
  helm install {{ .Release.Name }} ./charts/elassandra -f charts/elassandra/values-minikube.yaml
